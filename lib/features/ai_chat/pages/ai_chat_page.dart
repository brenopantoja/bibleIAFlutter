import 'package:flutter/material.dart';

class Ai_chatPage extends StatelessWidget {

  const Ai_chatPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Ai_chat Page'),
      ),
    );
  }

}
